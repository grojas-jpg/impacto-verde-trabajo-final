# Automatización Make

`escenario_make_V1_1.blueprint.json` reproduce la estructura del escenario final.

Al importarlo en otra cuenta de Make hay que volver a seleccionar las conexiones de Google y Gemini.
El archivo no incluye contraseñas ni claves API utilizables.
